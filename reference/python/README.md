# Scoutica Python reference models

Pydantic v2 reference models for the authoritative candidate profile, evidence, and Rules of Engagement schemas.

```bash
python3 -m pip install ./reference/python
```

The package requires Python 3.11+, Pydantic 2, and `jsonschema[format]`. The format extra is required so URI validation cannot silently degrade.
