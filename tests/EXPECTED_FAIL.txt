# tests/EXPECTED_FAIL.txt — expected-fail allowlist for SPRINT-AUDIT-REMEDIATION.
#
# One finding-id per line (inline "# ..." comments allowed). A FAIL whose finding-id is
# listed here is treated as XFAIL (known-open) and does NOT turn the suite red. When a
# finding is remediated, its regression test / gate invariant flips to PASS; run.sh then
# reports XPASS ("remove from EXPECTED_FAIL.txt"). Delete the id here in the same phase.
#
# This file MUST shrink to EMPTY by sprint end (final verdict matrix). At that point the
# security gate passes clean and every regression test passes with no allowlist entries.
#
# Seeded 2026-07-08 with all open findings from the authoritative ledger.

# ── Phase 1 — critical RCE ──
# F-CRIT-RCE-001 — RESOLVED: $response/payload path routed via argv/heredoc; gate INV-STATIC-INTERP-SCAN passes.

# ── Phase 2 — RESOLVED (scoring / compensation / verdicts / confirm / template) ──
# All fixed with regression tests in tests/scoring.test.sh + tests/cli_phase2.test.sh.
# NOTE F-MED-SCORE-004/005: soft-reject now surfaces a distinct manual-review signal instead of
# auto-rejecting (proposed-contract — awaiting maintainer sign-off on the verdict semantics; see report).

# ── Phase 3 — RESOLVED (fetch / publish / filesystem / preview / local-exec) ──
# All fixed + gated (security_gate.sh) and regression-tested (tests/phase3.test.sh):
# reusable _validate_url (fail-closed SSRF, re-checks discovered card_url), _refuse_if_symlink
# guard on state/identity/org writers, PII trap, no auto-publish, truthful copy, size-capped +
# JSON-validated saves, zero interpolated -c sites, safe_href scheme filter, reachable parse branch.

# ── Phase 4 — RESOLVED (schema ownership / engagement alias / docs / examples / housekeeping) ──
# freelance→contract normalizer + canonical 5-type enums across all homes; schema index + $id on
# recruiter/registry; network copy reworded to local simulation; mock names; AGENTS/README fixes;
# _archive backlinks de-linked. Regression tests in tests/phase4.test.sh.
# NOTE F-LOW-DOCS-002: SKILL.md length (266>150) left as-is (content-trim deferred; low, cosmetic).
# NOTE F-MED-HOUSE-001: the untracked/gitignored _archive/ dir is left on disk (removing local
# files autonomously is out of scope) — user may `rm -rf .specs/historic_prototype/_archive`.

# ── Phase 5 — RESOLVED (PowerShell + identity) ──
# PS publish stages an allowlist (no git add -A); PS init copies card.gitignore; PS generation
# routes scalars through ConvertTo-JsonScalar/ConvertTo-YamlScalar. identity init generates a real
# secp256k1 keypair (bech32 npub/nsec) or refuses cleanly — never placeholder keys; private key file
# is created atomically at 0600 and its dir at 0700. Regression tests in tests/phase5.test.sh.

# ═══════════════════════════════════════════════════════════════════════════════════════════════
# ALLOWLIST EMPTY — every audit finding is resolved with a passing gate/regression test.
# Sprint-completion condition (final verdict matrix) met.
