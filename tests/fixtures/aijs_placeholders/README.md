# Fixture: aijs_placeholders

The pristine upstream template with every field still an unfilled `[YOUR_*]` placeholder.
`scoutica import aijs` MUST abort (required fields are placeholders) and write ZERO files.
