# Hostile cwd schema fixture

These deliberately permissive schemas must never override Scoutica's trusted checkout or install
schemas merely because a command runs from this directory. They contain mock structure only.
