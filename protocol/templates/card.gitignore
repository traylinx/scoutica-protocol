# Scoutica — never commit these files
.scan_response_raw.txt
.backup/
.preview/
.scoutica/
scoutica_prompt.txt

# Application drafts from the apply-to-role skill (may reference private role details)
applications/

# Secrets and environment
*.env
.env*
secrets/

# OS files
.DS_Store
Thumbs.db
