---
title: "CLI Overview"
description: "Complete reference for all Scoutica CLI commands (v0.4.0)"
---

The Scoutica CLI is a single Bash script installed to `~/.scoutica/bin/scoutica`. It handles the full lifecycle of a Skill Card — from creation to validation, publishing, discovery, and now agent-to-agent messaging across the network.

<Note>
Scoutica is **server-less**. Every command runs locally against files you own; the network layer moves data through public Git repositories and pull requests, never a central broker. Your data never leaves your machine unless you publish it.
</Note>

## Help output

```text
Scoutica Protocol CLI v0.4.0
Your skills. Your rules. Your data.

Usage:  scoutica <command> [options] [directory]

🚀 Create your card:
  scan <docs-folder>   Auto-generate from your documents (easiest — just point at your CV folder)
  init                 Step-by-step interactive wizard
  init --ai            Generate via AI assistant (paste your CV)
  import aijs <fork>   Convert an ai-job-search fork into a card (offline, deterministic)

🔧 Manage your card:
  info     [dir]       View your card summary
  preview  [dir]       Build HTML layout and publish to here.now
  validate [dir]       Validate card against protocol schemas
  publish  [dir]       Push card to GitHub
  resolve  <url>       Fetch and display any card from a URL

🏢 Employer commands:
  org init             Create a Recruiter/Employer Identity Card
  org verify           Verify domain ownership (DNS TXT record)
  org publish          Push employer card to GitHub
  role create          Create a structured job posting (role.json)
  role validate [dir]  Validate role(s) against protocol schemas

🌐 Network commands:
  evaluate <card> <role>  Score fit between a candidate and role
  jobs search             Search the registry for candidates or roles
  send <url> --type ...    Send a message to another agent
  inbox                   Check for incoming messages
  reply <msg_id> --accept Accept/reject a message
  deliver                 Push pending messages to recipients
  register <dir> --type   Generate registry entry for PR submission
  identity init           Generate your Nostr keypair

⚙️  Advanced:
  doctor               System diagnostics and health check
  status               Show local card, identity, and network state
  logs                 Show recent CLI activity
  update               Update the Scoutica Protocol CLI
  help                 Show this help
  version              Show version
```

## Command summary

### Create & manage your card

| Command | Description |
|---------|-------------|
| [`init`](/cli/init) | Interactive wizard to create your Skill Card |
| [`import`](/cli/import) | Convert an ai-job-search fork into a Skill Card (offline, deterministic) |
| [`scan`](/cli/scan) | AI-powered card generation from your documents |
| [`validate`](/cli/validate) | Validate your card against the protocol schemas |
| [`publish`](/cli/publish) | Stage and push your card files to GitHub |
| [`preview`](/cli/preview) | Build an HTML preview and publish to here.now |
| [`resolve`](/cli/resolve) | Fetch and display a remote Skill Card by URL |
| [`info`](/cli/info) | Show a summary of your local card |

### Employer & roles

| Command | Description |
|---------|-------------|
| [`org`](/cli/org) | Scaffold, verify, and publish a Recruiter Card for employers |
| [`role`](/cli/role) | Create and validate structured job postings |

### Network & messaging

| Command | Description |
|---------|-------------|
| [`evaluate`](/cli/evaluate) | Score fit between a candidate card and a role (deterministic) |
| [`jobs`](/cli/jobs) | Search the registry for candidates or roles |
| [`send`](/cli/send) | Send a message to another agent |
| [`inbox`](/cli/inbox) | Check for incoming messages |
| [`reply`](/cli/reply) | Accept or reject a received message |
| [`deliver`](/cli/deliver) | Push pending messages to their recipients |
| [`register`](/cli/register) | Generate a registry entry for PR submission |
| [`identity`](/cli/identity) | Create and show your Nostr signing keypair |

### System

| Command | Description |
|---------|-------------|
| [`doctor`](/cli/doctor) | Run system diagnostics and check prerequisites |
| [`status`](/cli/status) | Show local card, identity, and network state |
| [`logs`](/cli/logs) | Show recent CLI activity |
| [`update`](/cli/update) | Update the CLI and templates to the latest version |

## All commands

<CardGroup cols={2}>
  <Card title="init" icon="wand-magic-sparkles" href="/cli/init">
    Create your Skill Card with an interactive wizard or AI-guided flow.
  </Card>
  <Card title="import" icon="file-import" href="/cli/import">
    Convert an ai-job-search fork into a Skill Card — offline and deterministic.
  </Card>
  <Card title="scan" icon="magnifying-glass" href="/cli/scan">
    Auto-generate a card from your CV, certificates, and portfolio using a local AI CLI.
  </Card>
  <Card title="validate" icon="circle-check" href="/cli/validate">
    Validate your card against the JSON Schema before publishing.
  </Card>
  <Card title="publish" icon="cloud-arrow-up" href="/cli/publish">
    Stage only canonical card files and push them to your GitHub repo.
  </Card>
  <Card title="preview" icon="eye" href="/cli/preview">
    Build an HTML page from your card data and publish it to here.now.
  </Card>
  <Card title="resolve" icon="link" href="/cli/resolve">
    Fetch and display any Skill Card from a public URL.
  </Card>
  <Card title="info" icon="circle-info" href="/cli/info">
    Show a quick summary of the card in the current directory.
  </Card>
  <Card title="org" icon="building" href="/cli/org">
    Scaffold a Recruiter Card, verify your organization's domain, and publish it.
  </Card>
  <Card title="role" icon="briefcase" href="/cli/role">
    Create structured `role.json` job postings and validate them against the schemas.
  </Card>
  <Card title="evaluate" icon="scale-balanced" href="/cli/evaluate">
    Score fit between a candidate and a role using the deterministic scorer.
  </Card>
  <Card title="jobs" icon="magnifying-glass-chart" href="/cli/jobs">
    Search the registry for candidates or roles.
  </Card>
  <Card title="send" icon="paper-plane" href="/cli/send">
    Send a message to another agent over the server-less network.
  </Card>
  <Card title="inbox" icon="inbox" href="/cli/inbox">
    Check for incoming messages addressed to you.
  </Card>
  <Card title="reply" icon="reply" href="/cli/reply">
    Accept or reject a message you received.
  </Card>
  <Card title="deliver" icon="truck-fast" href="/cli/deliver">
    Push your pending outbound messages to their recipients.
  </Card>
  <Card title="register" icon="clipboard-list" href="/cli/register">
    Generate a registry entry to submit your card via pull request.
  </Card>
  <Card title="identity" icon="key" href="/cli/identity">
    Create and inspect your local Nostr signing keypair.
  </Card>
  <Card title="doctor" icon="stethoscope" href="/cli/doctor">
    Run a full system check to verify all prerequisites are installed.
  </Card>
  <Card title="status" icon="gauge" href="/cli/status">
    Show your local card, identity, and network state at a glance.
  </Card>
  <Card title="logs" icon="list" href="/cli/logs">
    Review recent CLI activity.
  </Card>
  <Card title="update" icon="arrow-rotate-right" href="/cli/update">
    Re-run the installer to fetch the latest CLI and templates.
  </Card>
</CardGroup>

## Global conventions

Most card commands accept an optional `[directory]` argument. When omitted, the current working directory is used.

```bash
scoutica validate            # validates ./
scoutica validate ./my-card  # validates ./my-card/
```

Exit codes follow standard UNIX conventions: `0` for success, non-zero for failure. Commands that emit machine-readable output (for example `scoutica evaluate --json`) print clean JSON to stdout with no decorative banner, so they can be piped into other tools.
