---
title: "Quick Start"
description: "Create your Scoutica Skill Card in 5 minutes. Your skills, your rules, your data."
---

<Steps>
<Steps>
  <Step title="Native Agent Install (Most Recommended)">
    The quickest way to install and configure everything is to give your AI agent (like Claude Code, Cursor, OpenCode, or OpenClaw) a single instruction. The agent will read the protocol and execute the entire setup autonomously in the background.

    <Tabs>
      <Tab title="For Candidates">
        Copy this prompt and paste it into your AI Agent:
        ```text
        Read https://raw.githubusercontent.com/traylinx/scoutica-protocol/main/SKILL.md and follow the instructions to set up Scoutica Protocol natively.
        ```
        
        ### 🤖 AI Agent Simulation Log

        **1. Step 1: Install CLI**
        *   **AI executes bash:** `curl -fsSL https://raw.githubusercontent.com/traylinx/scoutica-protocol/main/install.sh | bash`
        *   *Terminal Output:* `✅ Scoutica installed successfully.`

        **2. Step 2: Locate files & Scan**
        *   **AI executes bash:** `cd ~/CV && scoutica scan .`
        *   *Terminal Output:* `Creating dedicated card folder: ~/CV/john-doe-card`

        **3. Step 3: Schema Validation**
        *   **AI executes bash:** `scoutica validate`
        *   *Terminal Output:* `✅ All files passed.`

        **4. Step 4: Network Registration & Publishing**
        *   **AI executes bash:** `scoutica register` and `scoutica publish`
        *   *Terminal Output:* `✅ Successfully published to GitHub! Registered on the Scoutica Network.`
      </Tab>
      <Tab title="For Recruiters">
        Copy this prompt and paste it into your AI Agent:
        ```text
        Read https://raw.githubusercontent.com/traylinx/scoutica-protocol/main/RECRUITER_SKILL.md and follow the instructions to set up the Scoutica Employer Network natively.
        ```

        ### 🤖 AI Agent Simulation Log

        **1. Step 1: Install CLI**
        *   **AI executes bash:** `curl -fsSL https://raw.githubusercontent.com/traylinx/scoutica-protocol/main/install.sh | bash`
        
        **2. Step 2: Scaffold Organization**
        *   **AI executes bash:** `scoutica org init`
        *   *Terminal Output:* `✅ Created recruiter_profile.json and hiring_rules.yaml.`

        **3. Step 3: Register and Search**
        *   **AI executes bash:** `scoutica org publish` and `scoutica jobs search`
        *   *Terminal Output:* `✅ Successfully published to GitHub! Found 14 matching candidates.`
      </Tab>
    </Tabs>
  </Step>

  <Step title="Manual CLI Install">
    If you prefer to install it yourself, run the installer:
    <Tabs>
      <Tab title="macOS / Linux">
        ```bash
        curl -fsSL https://raw.githubusercontent.com/traylinx/scoutica-protocol/main/install.sh | bash
        source ~/.zshrc   # or source ~/.bashrc
        ```
      </Tab>
      <Tab title="Windows (PowerShell)">
        ```powershell
        irm https://raw.githubusercontent.com/traylinx/scoutica-protocol/main/install.ps1 | iex
        ```
      </Tab>
    </Tabs>

    Then, build your card manually using one of these methods:
    <Tabs>
      <Tab title="AI Scan">
        Let AI generate your card automatically from your local documents.
        ```bash
        scoutica scan ~/my-docs/
        ```
        Use a specific AI provider explicitly:
        ```bash
        scoutica scan ~/my-docs/ --with gemini      # Gemini CLI
        scoutica scan ~/my-docs/ --with claude      # Claude Code
        scoutica scan ~/my-docs/ --with ollama      # Ollama (offline privacy)
        ```
      </Tab>
      <Tab title="Interactive Wizard">
        Answer questions step-by-step to build your card:
        ```bash
        scoutica init ./my-card/
        # Or get AI-guided instructions
        scoutica init --ai
        ```
      </Tab>
      <Tab title="Browser (No CLI)">
        Go to the [Scoutica Protocol repo](https://github.com/traylinx/scoutica-protocol) and copy `GENERATE_MY_CARD.md`. Paste it into ChatGPT or Claude web.
      </Tab>
    </Tabs>

    After creation, your card folder looks like this:
    ```
    my-card/
    ├── profile.json        # Your skills, experience, tools
    ├── rules.yaml          # Your rules of engagement
    ├── evidence.json       # Links to your public work
    └── SKILL.md            # Agent entry point
    ```
  </Step>

  <Step title="Validate your card">
    Run the schema validator to check your card for errors:

    ```bash
    scoutica validate ./my-card/
    ```

    Expected output:
    ```text
    ✅ 5 checks passed
    ```

    Fix any validation errors before publishing.
  </Step>

  <Step title="Publish to GitHub">
    Push your card to your GitHub repository:

    ```bash
    scoutica validate && scoutica publish
    ```

    Your card is now live and discoverable by any AI agent at your GitHub repo URL.

    <Note>
      `scoutica publish` only stages canonical card files (`profile.json`, `rules.yaml`, `evidence.json`, `scoutica.json`, `SKILL.md`). Your raw source documents are never committed.
    </Note>
  </Step>
</Steps>

---

## What's next

<CardGroup cols={2}>
  <Card title="CLI Reference" icon="terminal" href="/cli/overview">
    Full reference for all Scoutica CLI commands and flags
  </Card>
  <Card title="Your Skill Card" icon="id-card" href="/skill-card/overview">
    Understand the 4 files that make up your Skill Card
  </Card>
  <Card title="Privacy Zones" icon="lock" href="/guides/privacy-zones">
    How your three-zone privacy model works
  </Card>
  <Card title="Developer Guide" icon="code" href="/developer/overview">
    Build apps and integrations on the Scoutica Protocol
  </Card>
</CardGroup>
